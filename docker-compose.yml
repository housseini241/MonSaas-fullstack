services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  backend:
    build: ./backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      - MONGO_URL=mongodb://mongo:27017/artisanweb
      - OLLAMA_BASE_URL=http://172.17.0.1:11434
    volumes:
      - uploads_data:/app/uploads
      - static_sites_data:/app/static_sites
    ports:
      - "8000:8000"
    depends_on:
      - mongo


  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  mongo_data:
  uploads_data:
  static_sites_data:
